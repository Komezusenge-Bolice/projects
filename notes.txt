xml: extensive markup language
json: javascript object notation.
To convert json to javascript we use the format: JNSON.stringfy(user)
 and to convert javascript to json we use the format: JNSON.parse(user).

The main two key rules in json are: - no trailing commas in objects, - use double quotes only.

// the grobal functions available in nodejs are: 
- setTimeout()
- clearTimeout()
- setInterval()
- clearInterval()



the following are the common django-admin commands 
- check
    compilemessages
    createcachetable
    dbshell
    diffsettings
    dumpdata
    flush
    inspectdb
    loaddata
    makemessages
    makemigrations
    migrate
    optimizemigration
    runserver
    sendtestemail
    shell
    showmigrations
    sqlflush
    sqlmigrate
    sqlsequencereset
    squashmigrations
    startapp
    startproject
    test
    testserver


 a session is a permanent memory for managing users data on the server.